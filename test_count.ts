import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const count = await prisma.scheduleWorkflowTransition.count(); console.log('Transition count in DB:', count); } main(); 
